CREATE TABLE `Actors` (
  `ID` INTEGER,
  `Race` TEXT,
  `Class` TEXT,
  `Description` TEXT,
  `StartArea` TEXT,
  `StartPortal` TEXT,
  `MaleAnimationSet` INTEGER,
  `FemaleAnimationSet` INTEGER,
  `Scale` REAL,
  `Radius` REAL,
  `MaleMesh` INTEGER,
  `FemaleMesh` INTEGER,
  `GubbinData` BLOB,
  `Beards` BLOB,
  `MaleHair` BLOB,
  `FemaleHair` BLOB,
  `MaleFaceTEXTures` BLOB,
  `FemaleFaceTEXTures` BLOB,
  `MaleBodyTEXTures` BLOB,
  `FemaleBodyTEXTures` BLOB,
  `MaleSpeech` BLOB,
  `FemaleSpeech` BLOB,
  `BloodTEXTure` INTEGER,
  `Attributes` BLOB,
  `Resistances` BLOB,
  `Genders` INTEGER,
  `Playable` INTEGER,
  `Ridable` INTEGER,
  `Aggression` INTEGER,
  `AggressionRange` INTEGER,
  `TradeMode` INTEGER,
  `EnvironmentType` INTEGER,
  `InventorySlots` INTEGER,
  `DefaultDamageType` INTEGER,
  `DefaultFaction` INTEGER,
  `XPMultiplier` INTEGER,
  `PolygonCollision` INTEGER
)